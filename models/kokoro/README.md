# Kokoro-82M Model

Kokoro-82M is a highly lightweight (82 million parameters) TTS model delivering exceptional speed, natural prosody, and ultra-low VRAM usage on local GPUs.

## Setup & Running
- Run `install.bat` to install requirements.
- Run `run.bat` to perform synthesis.
